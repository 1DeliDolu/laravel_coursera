# 🔐 Şifreleme

Laravel bazı dahili mekanizmalarda şifreleme kullanır ve kendi kodunuzda da aynı sistemi kullanabilmeniz için birkaç hızlı facade metodu sunar. Laravel, OpenSSL, AES-256 şifreleme kullanır ve tüm değerler MAC ile imzalanır. Şifreleme için gerekli kriptografik anahtarlar genellikle uygulama ilk kurulduğunda oluşturulur. Bu değer, environment değişkeni olan app key içinde veya config içindeki app.key değerindedir. Bunun için endişelenmenize gerek yoktur, ancak bu anahtarı kaybetmeyin; aksi hâlde gelecekte verileri decrypt edemezsiniz.

Laravel, cookies değerlerini varsayılan olarak kendi şifreleme mekanizmasıyla şifreler. Ayrıca bu mekanizmaya crypt facade’ı ile doğrudan erişebilirsiniz. Crypt facade’ında programcıların en çok kullanacağı iki metot encrypt string ve decrypt string metodlarıdır. Bir string’i encrypt string metoduna gönderirsiniz ve bunun şifrelenmiş hâlini alırsınız; decrypt string metodu ile de bunun çözülmüş hâline ulaşırsınız — bu kadar basit.

Daha fazla şifreleme yeteneği arıyorsanız Laravel’in ötesine bakmanız gerekir çünkü bu framework’ün odağı gerçekten bu değildir. PHP içinde geliştiricilerin libsodium’u nasıl uyguladığını incelemenizi öneririm.

Laravel bazı dahili mekanizmalarda şifreleme kullanır ve kendi kodunuzda da aynı sistemi kullanabilmeniz için birkaç hızlı facade metodu sunar. Laravel, OpenSSL, AES-256 şifreleme kullanır ve tüm değerler MAC ile imzalanır. Şifreleme için gerekli kriptografik anahtarlar genellikle uygulama ilk kurulduğunda oluşturulur. Bu değer, environment değişkeni olan app key içinde veya config içindeki app.key değerindedir. Bunun için endişelenmenize gerek yoktur, ancak bu anahtarı kaybetmeyin; aksi hâlde gelecekte verileri decrypt edemezsiniz. Laravel, cookies değerlerini varsayılan olarak kendi şifreleme mekanizmasıyla şifreler. Ayrıca bu mekanizmaya crypt facade’ı ile doğrudan erişebilirsiniz. Crypt facade’ında programcıların en çok kullanacağı iki metot encrypt string ve decrypt string metodlarıdır. Bir string’i encrypt string metoduna gönderirsiniz ve şifrelenmiş hâlini alırsınız, decrypt string metodu ile de bunun çözülmüş hâline ulaşırsınız — bu kadar basit.

Daha fazla şifreleme yeteneği arıyorsanız Laravel’in ötesine bakmanız gerekir çünkü bu framework’ün odağı gerçekten bu değildir. PHP içinde geliştiricilerin libsodium’u nasıl uyguladığını incelemenizi öneririm.

Laravel bazı dahili mekanizmalarda şifreleme kullanır ve kendi kodunuzda da aynı sistemi kullanabilmeniz için birkaç hızlı facade metodu sunar. Laravel, OpenSSL, AES-256 şifreleme kullanır ve tüm değerler MAC ile imzalanır. Şifreleme için gerekli kriptografik anahtarlar genellikle uygulama ilk kurulduğunda oluşturulur. Bu değer, environment değişkeni olan app key içinde veya config içindeki app.key değerindedir. Bunun için endişelenmenize gerek yoktur, ancak bu anahtarı kaybetmeyin; aksi hâlde gelecekte verileri decrypt edemezsiniz. Laravel, cookies değerlerini varsayılan olarak kendi şifreleme mekanizmasıyla şifreler. Ayrıca bu mekanizmaya crypt facade’ı ile doğrudan erişebilirsiniz. Crypt facade’ında programcıların en çok kullanacağı iki metot encrypt string ve decrypt string metodlarıdır. Bir string’i encrypt string metoduna gönderirsiniz ve şifrelenmiş hâlini alırsınız; decrypt string metodu ile de bunun çözülmüş hâline ulaşırsınız — bu kadar basit.

Daha fazla şifreleme yeteneği arıyorsanız Laravel’in ötesine bakmanız gerekir çünkü bu framework’ün odağı gerçekten bu değildir. PHP içinde geliştiricilerin libsodium’u nasıl uyguladığını incelemenizi öneririm.
