# 🧭 Ortam ve Debug Etkileri

Önceki bir videoda, geçerli deploy ortamını yapılandırmak için kullanılan environment variable’lardan ve uygulamanızın debug modunda olup olmadığından bahsetmiştim. Bunun üzerinde tekrar durmak istiyorum çünkü gerçekten çok önemli. Birçok saldırı vektörü, yanlış yapılandırılmış bir ortamdan veya sistemle ilgili detayların sızmasından kaynaklanır. Ortam değişkenimiz olan app env; production, testing ve local olarak ayarlanabilir. Bunlar sisteme yerleşik değerlerdir. Elbette kullandığınız sistem ortamına göre bunları istediğiniz başka değerlere de genişletebilirsiniz. Debug durumu ise açıktır veya kapalıdır — true veya false.

Gördüğümüz gibi, bu değerler framework kodunda middleware, exception handler gibi yerlerde kullanılır. Üçüncü taraf plug-in’lerde de kullanılabilirler. Daha önce söylediğim gibi, bu açık kaynak plug-in’leri incelemeniz ve app environment veya debug moduna referans olup olmadığını kontrol etmeniz gerekir.

Hemen akla gelmeyebilecek başka bir alan ise blade dosyaları veya live view’lerdir. Bunun neden önemli olduğuna bakalım. Burada, gerçek bir müşterinin projesinin kurgulanmış bir kopyasını oluşturdum. Tüm error sayfalarını yayınladıklarını görebilirsiniz. Şimdi onların 500 error sayfasına bakalım. Eğer daha yeni bir Laravel sürümü kullanıyorsanız, bu blade dosyasında yapılan şey size yabancı gelebilir. Yeni sürümler whoops error handler kullanır ve bu nedenle bir blade dosyasında exception bulunması söz konusu olmaz. Ancak bazı projeler uzun ömürlüdür ve pek çok Laravel sürümünden geçerek yükseltilmiştir. Bu proje eski bir Laravel sürümünden oluşturulmuştu. Şimdi whoops error handler kullanıyor ama bu eski kod hâlâ orada duruyor. Peki gerçekten hiç çalışmıyor mu?

Local ortamda çalıştırıldığında, exception handler bu view’lerin render edilmesini tamamen atladığı için hiçbir zaman gösterilmez. Ancak production ortamında, yine bu dosyalara ulaşır ve apt debug false olduğu için güvenli view render edilir. Peki production’da bir şeyi debug etmeye çalışırken app debug’u açarsanız ne olur? Aynı anda bir başka kullanıcı sitede bir hata oluşturabilir ve bu durumda tüm exception dump’ını — secrets dahil — görür. Bu hiç iyi değildir.

Bu ayarlar tasarım gereği harikadır, ancak her zaman proje kodunun tamamını doğrulayıp beklendiği gibi kullanıldıklarından emin olmak kritik önem taşır.
