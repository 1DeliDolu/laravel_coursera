# 🔒 SSL Zorunlu Kılma

Bu videoda, bir Laravel uygulamasında SSL’i zorunlu kılmaktan bahsedeceğiz. Laravel uygulamanıza gelen tüm isteklerin SSL üzerinden gelmesini sağlamak harika bir fikirdir. Bu nedenle projede SSL’i zorunlu kılmanın en iyi yöntemlerine bakalım. DNS için Cloudflare gibi bir şey kullanıyorsanız, isteklerinizi onların üzerinden proxy’leyebilirsiniz. Cloudflare rules adı verilen bir özellik vardır; bu özellik kenarda (edge) SSL olmayan tüm istekleri SSL sürümüne yönlendiren kurallar oluşturmanıza izin verir. Aksi hâlde, bunu bir edge web sunucusunda, load balancer’da veya Apache ya da NGINX yapılandırmanızda ayarlamak her zaman en iyisidir. Ancak bunu yaptıktan sonra env dosyanızda yer alan app URL değerinin SSL sürümünü kullanacak şekilde ayarlandığından emin olmalısınız.

Bu, kodunuzun mutlak URL kullandığı durumlarda SSL sürümüne doğrudan yönlendirilmesi için faydalıdır ve gereksiz yönlendirmeleri ortadan kaldırır. Projenizde herhangi bir nedenle Cloudflare kullanamıyorsanız veya sunucuda bir yönlendirme yapma şansınız yoksa, Laravel Middleware kullanarak her istekte SSL’i zorunlu kılabilirsiniz. Bir seçenek, bir yönlendirme zorlamaktır; yani istek SSL değilse SSL sürümüne yönlendirilir. Ancak bu yöntem POST isteklerinde iyi çalışmaz, çünkü yönlendirme POST verilerini korumaz. Diğer bir seçenek ise isteği 400 hatasıyla reddetmektir; bu da kullanıcı deneyimi açısından pek iyi değildir. Şimdi her iki yöntemi nasıl yapabileceğimize bakalım.

İlk olarak, bu force SSL with redirect middleware’i görüyoruz. Bu middleware, request ve next closure alır ve diğer tüm middleware’lerde olduğu gibi çalışır. İstek güvenli mi diye kontrol eder. Eğer değilse, işlemi durdurur ve aynı URL’ye (query parametreleri dahil) güvenli bir şekilde yönlendirme döndürür. Aksi hâlde next callback ile isteği normal şekilde işler. enforce SSL only sürümü de neredeyse aynı şekilde çalışır. İsteğin güvenli olup olmadığını kontrol eder ancak güvenli değilse işlemi durdurup 400 hatası döner. Güvenliyse normal şekilde devam eder.

Kullanmak istediğiniz yönteme veya middleware’e bağlı olarak bunu Http kernel’de global middleware stack’in en üstüne kaydedersiniz. Bu, diğer tüm işlemlerden önce çalışacağı anlamına gelir. Benim örneğimde, daha iyi bir kullanıcı deneyimi sunduğu için force SSL with redirect middleware’i kaydediyorum.
